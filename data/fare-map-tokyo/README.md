# FareMapTokyo fare data

Place the local FareMapTokyo-compatible fare graph here:

- `fare_graph.json`

This folder is intentionally small in source control. The search fare logger can
run without the full file and will report `missing-data` diagnostics instead of
blocking journey search.
